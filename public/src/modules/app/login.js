import { $api, $form, $route } from '#';

@mixin(['on-change'])
export default class extends React.Component {
  render() {
    return (
      <div className="login-wrapper">
        LOGIN
      </div>
    );
  }
}
